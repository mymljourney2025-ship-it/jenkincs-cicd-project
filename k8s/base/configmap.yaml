apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-config
  namespace: application
  labels:
    app: myapp
data:
  NODE_ENV: "production"
  PORT: "3000"
  LOG_LEVEL: "info"
  APP_VERSION: "1.0.0"
